groups:
  - name: MLOpsAlerts
    rules:
      - alert: ModelConfidenceDrift
        expr: prediction_confidence_score < 0.623
        for: 1m
        labels:
          severity: critical
        annotations:
          summary: "Model confidence drift detected on unstable release slot"
